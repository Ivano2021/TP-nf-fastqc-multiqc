// nextflow.config (BASE)

docker.enabled = true
// Apple Silicon workaround (muchas imágenes bioinfo son amd64)
docker.runOptions = '--platform=linux/amd64'

process.executor = 'local'
process.cpus = 2
process.memory = '4 GB'
